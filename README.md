# King Poker Backend

## PostgreSQL (Docker Compose)

This project includes a local PostgreSQL service for development.

### Quick start

```zsh
docker compose up -d
```

### Environment

Defaults live in `.env`. Adjust as needed:

- `POSTGRES_USER`
- `POSTGRES_PASSWORD`
- `POSTGRES_DB`

### Stop

```zsh
docker compose down
```
